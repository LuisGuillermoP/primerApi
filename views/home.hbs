{{!-- el home sirve como instrumento para ver que se va a ver dentro de la plantilla --}}
<h1>Agregue sus URL</h1>
{{>form}}
{{#each urls }}
    {{>card item=this}}
    {{!-- si queremos podemos renombrar la variable ponemos el nuevo nombre = this --}}
{{/each}}